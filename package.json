{
  "name": "fechallenge",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "json-server --watch db/db.json --port 3001"
  },
  "devDependencies": {
    "json-server": "1.0.0-beta.13"
  }
}
